# Vendor Features Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Vue.js Frontend Application                     │  │
│  │  ┌────────────┐  ┌──────────┐  ┌────────┐  ┌───────────┐   │  │
│  │  │ Dashboard  │  │ Products │  │ Orders │  │   Stock   │   │  │
│  │  │  View      │  │  View    │  │  View  │  │   View    │   │  │
│  │  └─────┬──────┘  └────┬─────┘  └───┬────┘  └─────┬─────┘   │  │
│  │        │              │            │              │         │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │         Router (Vue Router)                         │   │  │
│  │  │  /vendor/dashboard, /vendor/products, etc.         │   │  │
│  │  └──────────────────────────────────────────────────┬──┘   │  │
│  │                                                      │       │  │
│  │  ┌──────────────────────────────────────────┬───────┴────┐  │  │
│  │  │         Sidebar Navigation               │ Page View  │  │  │
│  │  │  - Dashboard   📊                        │            │  │  │
│  │  │  - Products    📦                        │ (Rendered  │  │  │
│  │  │  - Clients     👥                        │  by        │  │  │
│  │  │  - Orders      📋                        │  RouterView)  │  │
│  │  │  - Stock       📈                        │            │  │  │
│  │  │  - Logout      🚪                        │            │  │  │
│  │  └────────────────────────────────────────┴────────────┘  │  │
│  │                                                             │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │        Reusable Components                           │  │  │
│  │  │  - StatsCard.vue (statistics display)               │  │  │
│  │  │  - StatusBar.vue (progress bar)                     │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                              │                                       │
│                              ▼                                       │
│                    HTTP/HTTPS Requests                              │
│                   (with Bearer Token)                               │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────────────┐
│                      API Gateway / Router                             │
│          (backend/routes/api.php - vendor routes)                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Authentication Middleware (auth:sanctum)                   │  │
│  │  - Verify Bearer Token                                      │  │
│  │  - Load authenticated user                                  │  │
│  └────────────────────────────┬─────────────────────────────────┘  │
│                                │                                     │
│  ┌──────────────────────────────▼─────────────────────────────────┐ │
│  │  Authorization Middleware (vendor.approved)                    │ │
│  │  - Check vendor_status = 'approved'                           │ │
│  │  - Ensure vendor is active                                    │ │
│  └────────────────────────────┬─────────────────────────────────┘ │
│                                │                                     │
│  ┌──────────────────────────────▼─────────────────────────────────┐ │
│  │  Route Handler - Select Controller                            │ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │          /vendor/products Routes                        │ │ │
│  │  │  ├─→ ProductController@index (GET)                     │ │ │
│  │  │  ├─→ ProductController@store (POST)                    │ │ │
│  │  │  ├─→ ProductController@show (GET)                      │ │ │
│  │  │  ├─→ ProductController@update (PUT)                    │ │ │
│  │  │  ├─→ ProductController@destroy (DELETE)                │ │ │
│  │  │  └─→ ... more product endpoints                        │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │          /vendor/clients Routes                         │ │ │
│  │  │  ├─→ ClientController@index (GET)                      │ │ │
│  │  │  ├─→ ClientController@show (GET)                       │ │ │
│  │  │  ├─→ ClientController@statistics (GET)                 │ │ │
│  │  │  └─→ ... more client endpoints                         │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │          /vendor/orders Routes                          │ │ │
│  │  │  ├─→ OrderController@index (GET)                       │ │ │
│  │  │  ├─→ OrderController@show (GET)                        │ │ │
│  │  │  ├─→ OrderController@updateStatus (PUT)                │ │ │
│  │  │  └─→ ... more order endpoints                          │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │          /vendor/stock Routes                           │ │ │
│  │  │  ├─→ StockController@index (GET)                       │ │ │
│  │  │  ├─→ StockController@update (PUT)                      │ │ │
│  │  │  ├─→ StockController@adjust (POST)                     │ │ │
│  │  │  └─→ ... more stock endpoints                          │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Laravel Backend Controllers                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  ProductController                                         │  │
│  │  - Queries VintageProduct model                           │  │
│  │  - Filters by vendor_id                                    │  │
│  │  - Returns JSON responses                                  │  │
│  │  - Validates input data                                    │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  ClientController                                          │  │
│  │  - Queries User model (role='client')                     │  │
│  │  - Filters purchases by vendor_id                          │  │
│  │  - Calculates statistics                                   │  │
│  │  - Generates CSV exports                                   │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  OrderController                                           │  │
│  │  - Queries Order & OrderItem models                       │  │
│  │  - Filters by vendor's products                            │  │
│  │  - Updates order status                                    │  │
│  │  - Exports order data                                      │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  StockController                                           │  │
│  │  - Queries VintageProduct model                           │  │
│  │  - Logs to stock_movements table                           │  │
│  │  - Calculates inventory value                              │  │
│  │  - Tracks stock history                                    │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Database Layer                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  Tables Used:                                                        │
│  ┌────────────────────┐  ┌────────────────────┐                    │
│  │  users             │  │ vintage_products   │                    │
│  │  - id (UUID)       │  │ - id (UUID)        │                    │
│  │  - name            │  │ - vendeur_id (FK)  │                    │
│  │  - email           │  │ - title            │                    │
│  │  - phone           │  │ - description      │                    │
│  │  - role            │  │ - category         │                    │
│  │  - vendor_status   │  │ - price            │                    │
│  │  - city            │  │ - promotion        │                    │
│  └────────────────────┘  │ - stock            │                    │
│                          │ - status           │                    │
│                          │ - condition        │                    │
│                          └────────────────────┘                    │
│                                                                      │
│  ┌────────────────────┐  ┌────────────────────┐                    │
│  │  orders            │  │  order_items       │                    │
│  │  - id (UUID)       │  │  - id              │                    │
│  │  - user_id (FK)    │  │  - order_id (FK)   │                    │
│  │  - total_price     │  │  - product_id (FK) │                    │
│  │  - status          │  │  - quantity        │                    │
│  │  - created_at      │  │  - total_price     │                    │
│  └────────────────────┘  └────────────────────┘                    │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  stock_movements (NEW)                                      │ │
│  │  - id (BIGINT)                                             │ │
│  │  - product_id (UUID FK)                                    │ │
│  │  - quantity (INT)                                          │ │
│  │  - reason (ENUM)                                           │ │
│  │  - notes (TEXT)                                            │ │
│  │  - created_at / updated_at                                 │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
User Action (Frontend)
        │
        ▼
Emit Event / API Call
        │
        ▼
Send HTTP Request with Auth Token
        │
        ▼
┌─────────────────────────────────────────────────┐
│  Laravel API Receives Request                   │
│  1. Validate token (auth middleware)            │
│  2. Check vendor status (authorization)         │
│  3. Route to controller                         │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │ Controller Method Executes  │
        │ 1. Validate input           │
        │ 2. Query database           │
        │ 3. Apply business logic     │
        │ 4. Format response          │
        └────────────┬────────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │ Database Query Execution   │
        │ 1. Filter by vendor_id     │
        │ 2. Apply conditions        │
        │ 3. Join related tables     │
        │ 4. Return results          │
        └────────────┬────────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │ Controller Formats Response│
        │ - JSON structure           │
        │ - Data transformation      │
        │ - Status codes             │
        └────────────┬────────────────┘
                     │
                     ▼
Send HTTP Response (JSON)
        │
        ▼
Frontend Receives Response
        │
        ▼
┌─────────────────────────────────────┐
│ Vue Component Processes Data        │
│ 1. Update reactive state            │
│ 2. Re-render UI                     │
│ 3. Show success/error message       │
└─────────────────────────────────────┘
        │
        ▼
User Sees Updated Content
```

## Component Relationship Diagram

```
DashboardView.vue (Layout)
│
├─→ Sidebar Navigation
│   ├─→ RouterLink (/vendor/dashboard)
│   ├─→ RouterLink (/vendor/products)
│   ├─→ RouterLink (/vendor/clients)
│   ├─→ RouterLink (/vendor/orders)
│   └─→ RouterLink (/vendor/stock)
│
├─→ Header
│   └─→ Page Title (dynamic based on route)
│
└─→ RouterView (Nested Routes)
    │
    ├─→ ProductsView.vue
    │   ├─→ StatsCard (for display)
    │   └─→ ProductModal
    │
    ├─→ ClientsView.vue
    │   ├─→ StatsCard
    │   └─→ ClientDetailsModal
    │
    ├─→ OrdersView.vue
    │   ├─→ StatsCard
    │   ├─→ OrderDetailsModal
    │   └─→ StatusUpdateModal
    │
    └─→ StockView.vue
        ├─→ StatsCard
        ├─→ AlertBar
        ├─→ EditStockModal
        └─→ AdjustStockModal
```

## Feature Interaction Flow

```
Vendor Workflow:

1. LOGIN
   └─→ /login → AuthController@login
   └─→ Store token
   └─→ Redirect to /vendor/dashboard

2. VIEW DASHBOARD
   └─→ /vendor/dashboard
   └─→ Load statistics from multiple endpoints
   └─→ Display overview cards

3. MANAGE PRODUCTS
   └─→ /vendor/products
   ├─→ GET /api/vendor/products (list)
   ├─→ POST /api/vendor/products (create)
   ├─→ PUT /api/vendor/products/{id} (update)
   └─→ DELETE /api/vendor/products/{id} (delete)

4. TRACK CLIENTS
   └─→ /vendor/clients
   ├─→ GET /api/vendor/clients (list)
   ├─→ GET /api/vendor/clients/{id} (details)
   ├─→ GET /api/vendor/clients/statistics
   └─→ GET /api/vendor/clients/export

5. MANAGE ORDERS
   └─→ /vendor/orders
   ├─→ GET /api/vendor/orders (list)
   ├─→ PUT /api/vendor/orders/{id}/status (update)
   ├─→ GET /api/vendor/orders/statistics
   └─→ GET /api/vendor/orders/export

6. MANAGE STOCK
   └─→ /vendor/stock
   ├─→ GET /api/vendor/stock (list)
   ├─→ PUT /api/vendor/stock/{id} (update)
   ├─→ POST /api/vendor/stock/{id}/adjust (adjust)
   ├─→ GET /api/vendor/stock/statistics
   ├─→ GET /api/vendor/stock/alerts
   └─→ GET /api/vendor/stock/export

7. LOGOUT
   └─→ POST /api/auth/logout
   └─→ Clear token
   └─→ Redirect to /login
```

## Security Layers

```
REQUEST
  │
  ▼
┌──────────────────────────────┐
│ HTTPS/TLS Encryption         │
│ (Transport Security)         │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Bearer Token Validation      │
│ (auth:sanctum middleware)    │
│ - Token present?             │
│ - Token valid?               │
│ - Token not expired?         │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ User Authorization           │
│ (vendor.approved middleware) │
│ - User is vendor?            │
│ - Vendor approved?           │
│ - Vendor not suspended?      │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Data Authorization           │
│ (Controller checks)          │
│ - Own products only          │
│ - Own clients only           │
│ - Own orders only            │
│ - Own stock only             │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│ Input Validation             │
│ - Type checking              │
│ - Range checking             │
│ - Enum validation            │
│ - SQL injection prevention   │
└──────────────┬───────────────┘
               │
               ▼
      DATABASE QUERY
```

---

**Last Updated:** January 6, 2026
**Architecture Version:** 1.0
**Status:** Production Ready ✅
